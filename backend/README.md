# Nivra Backend

Go API using Gin, GORM, PostgreSQL, Redis, JWT access tokens, refresh token rotation, and structured logging.

## Run

```bash
go mod download
go run ./cmd/api
```

The API expects the environment values documented in the root `.env.example`.

