# Nivra Frontend

Next.js app using TypeScript, Tailwind CSS, shadcn-style primitives, and lucide-react icons.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` to the backend API base URL. The default local API is `http://localhost:8080/api/v1`.

